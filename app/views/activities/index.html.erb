<h1> User's Activities </h1>

<br/>

<% @activities.each do |activity| %>
	<div class="activity">
	<%= link_to activity.owner.pseudo, activity.owner if activity.owner %>
	<%= render_activity activity %>
	</div>
<% end %>